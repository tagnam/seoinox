// const lenis = new Lenis()

// lenis.on('scroll', (e) => {
//   console.log(e)
// })

// function raf(time) {
//   lenis.raf(time)
//   requestAnimationFrame(raf)
// }

// requestAnimationFrame(raf)



new SilkeSlider('.silke-slider', {
    loop: true,
    autoplay: true,
    autoplaySpeed: 3000,
    navigation: true,
    pagination: true
  });